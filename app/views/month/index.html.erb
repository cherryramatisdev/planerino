<div class="grid grid-cols-3 gap-5">
  <% @months.each do |month| %>
    <div class="w-40 h-40 p-5 bg-gray-300 flex items-center justify-center rounded-lg">
      <%= month.name %>
    </div>
  <% end %>
</div>
